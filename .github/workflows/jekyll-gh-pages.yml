#将Jekyll站点构建和部署到GitHub页面的示例工作流
名称：部署预先安装GitHub页面相依性的Jekyll: 预先安装GitHub页面相依性来部署杰基尔

在……之上：:
#在以默认分支为目标的推送上运行#在以默认分支为目标的推送上运行
推：:
分支机构：["main"]: ["主要"]

#允许您从“操作”选项卡手动运行此工作流#允许您从[动作]索引标签手动执行此工作流程
工作流程分派(_D)：:

#设置GitHub_TOKEN的权限以允许部署到GitHub页面
权限：:
内容：已读: 读
页面：写入: 写
id-token：写入: 写

#只允许一个并发部署，跳过正在运行和最新排队之间排队的运行。
#但是，不要取消正在进行的运行，因为我们希望允许这些生产部署完成。
并发性:
组: "页面"
正在取消: 假的

工作:
  #生成作业
建立:
连续的：Ubuntu-最新
连续的：Ubuntu-最新-
-姓名：结帐
uses：操作/结帐@v4
      - uses：操作/结帐@v4: 安装页面
uses: 操作/configure-pages@v5
-姓名: uses用哲基尔构建
uses: 和……一起actions/jekyll-build-pages@v1
和……一起:
来源: ./
目的地：./_地点：./_站点
目的地：./_地点：./_站点: 上传工件
uses: action/upload-pages-artifact@v3

  #部署作业
 部署:
环境:
URL
URL: ${{steps.deployment.outputs.page_url}}
连续的: ubuntu-最新
buntu-最新: 建立
步骤:
      - 姓名: 部署到GitHub页面
身份标识: 部署
uses: 操作/部署页面@v5
